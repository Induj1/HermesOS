# Generated or vendored — never hand-formatted.
node_modules/
dist/
build/
coverage/
pnpm-lock.yaml
.pnpm-store/

# The Telegram agent's runtime workspace — files it writes, not source.
hermes-workspace/
hermes-data/

# Prettier's YAML printer rewrites the anchors and merge keys in
# docker-compose.yml, and its Markdown printer reflows the docs that were
# written to a deliberate width. Both are maintained by hand.
docker-compose.yml

# Not Prettier's languages.
*.sql
Justfile
hermes-docs/
